using System;
using UnityEditor;
using UnityEngine;
using UnityEngine.SceneManagement;

namespace DefaultNamespace.ScreenshotControlPart
{
    
    public class ScrStarterScene:MonoBehaviour
    {
        
        public void Start()
        {
            
            /*if(EditorApplication.isPlaying)
                return;
            SceneManager.LoadSceneAsync("Pathfinder_Interface",LoadSceneMode.Additive);
            SceneManager.LoadSceneAsync("GLscene",LoadSceneMode.Additive);*/
        }
    }
}